# ============================================================
# requirements.txt
# Proyecto: Brecha Digital y Discapacidad en España
# Instalación: pip install -r requirements.txt
# Python >= 3.11
# ============================================================

# ── Análisis y manipulación de datos ─────────────────────────
pandas>=2.0.0          # pipeline de limpieza, feature engineering y EDA
numpy>=1.26.0          # operaciones numéricas, np.where, manejo de NaN

# ── Visualización ─────────────────────────────────────────────
matplotlib>=3.7.0      # generación de las 8 figuras analíticas del proyecto
seaborn>=0.12.0        # heatmaps y boxplots en los notebooks de EDA

# ── Machine Learning ──────────────────────────────────────────
scikit-learn>=1.3.0    # K-Means clustering (05_clustering_paises.ipynb)
                       # StandardScaler para normalización de features

# ── Notebooks ─────────────────────────────────────────────────
jupyter>=1.0.0         # entorno interactivo para notebooks .ipynb
ipykernel>=6.0.0       # kernel Python para Jupyter y VS Code

# ── Exportación y base de datos ───────────────────────────────
openpyxl>=3.1.0        # exportación a .xlsx para tablas de resultados
sqlalchemy>=2.0.0      # ORM y conexión SQL para pandas (pd.read_sql, to_sql)

# ── Utilidades ────────────────────────────────────────────────
python-dotenv>=1.0.0   # gestión de variables de entorno (.env)

# ── Nota sobre SQLite ─────────────────────────────────────────
# sqlite3 es un módulo estándar de Python — NO requiere instalación
# proyecto.db se crea con: python python/sql_loader.py